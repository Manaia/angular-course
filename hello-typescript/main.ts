var variavel = 'variável';

function funcao (x, y) {
    return x + y;
}

let num = 2;
const PI = 3.14;

var numeros = [1, 2, 3];

numeros.map(function(v){
    return v * 2;
});

numeros.map(v => v * 2)

class Matematica {
    soma(x, y){
        return x + y;
    }
}

